# TurtleBot3 Pose Controller Package

This is a ROS2 Python package implementing a non-linear polar coordinate pose controller for TurtleBot3, designed to simultaneously drive and rotate to target coo>

## Prerequisites & Installation

1. **Install ROS2 dependencies:**
   Ensure you have configured your environment to work with TurtleBot3 Gazebo simulations.
```bash
   sudo apt update
   sudo apt install python3-transforms3d
   export TURTLEBOT3_MODEL=burger
